const environment = process.env.environment || 'dev';
// environment === 'dev' ? 'http://localhost:8080' :
export const apiConfig = {
    // url :  'http://localhost:8080'
    url :  'https://terra-scribe-api.vercel.app'
}
